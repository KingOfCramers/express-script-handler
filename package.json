{
  "scripts": {
    "test": "jest --watch",
    "dev": "pm2 flush && NODE_ENV=development pm2 reload ecosystem.config.js && pm2 save && pm2 logs",
    "clear": "pm2 delete API && pm2 save && pm2 flush",
    "prod": "pm2 flush && NODE_ENV=production pm2 reload ecosystem.config.js && pm2 save && pm2 monit",
    "prod:deploy": "NODE_ENV=production pm2 deploy ecosystem.config.js production"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "chalk": "^3.0.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "moment": "^2.24.0",
    "mongoose": "^5.9.4",
    "morgan": "^1.9.1",
    "object-sizeof": "^1.5.3",
    "redis": "^3.0.2",
    "winston": "^3.2.1"
  },
  "eslintConfig": {},
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-plugin-jest": "^23.8.2",
    "jest": "^25.1.0",
    "supertest": "^4.0.2"
  }
}
